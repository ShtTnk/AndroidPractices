<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 通知のパーミッション   -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- サービスをフォアグラウンドで起動するためのパーミッション  -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 通知のPendingIntentを使用するため   -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Dark"
        tools:targetApi="31">

        <!-- ウィザードが自動作成してくれたもの。nameは名前。enabledは登録したサービスを利用可能とするかどうか。Exportedはサービスを外部アプリケーションから利用可能にするかどうか-->
        <!-- android:foregroundServiceType="mediaPlayback" />はPendingIntentでアクティビティを起動するために必要 -->
        <service
            android:name=".SoundManageService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
